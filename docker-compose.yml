version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./outputs:/app/outputs
      - ./jobs_store:/app/jobs_store
    env_file:
      - .env
    restart: always
